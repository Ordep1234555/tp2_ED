#include <cstdio>
#include <iostream>
#include "string.h"
#include "headers/Quicksort.hpp"

using namespace std;

int main() {
  
  int num_planetas;
  cin >> num_planetas;
  int aux;
  for (aux = 0; aux < num_planetas; aux++)
    {
      int nome_p, dist_p, popu_p;
      cin >> nome_p >> dista_p >> popu_p;
  // Talvez um vetor?
    }
  return 0;
}
